<h1>Listing tipo_consultas</h1>

<table>
  <tr>
    <th>Tipo</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tipo_consultas.each do |tipo_consulta| %>
  <tr>
    <td><%= tipo_consulta.tipo %></td>
    <td><%= tipo_consulta.status %></td>
    <td><%= link_to 'Show', tipo_consulta %></td>
    <td><%= link_to 'Edit', edit_tipo_consulta_path(tipo_consulta) %></td>
    <td><%= link_to 'Destroy', tipo_consulta, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tipo consulta', new_tipo_consulta_path %>
